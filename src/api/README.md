# src/api

Core runtime adapters and boundary modules for loading, transport, diagnostics, and browser integration.

Key areas:
- asset and sound loaders
- transport adapters (PeerJS/WebSocket/lazy transport)
- error reporting and worker message contracts
